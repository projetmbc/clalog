# NOTES.
#     1) Names followed by one star indicates an optional setting.
#     2) Special comments ''#: ... :#'' allow to use one small DSL
#        to indicate some constraints in the specifications but not
#        the YAML file that will be used.


# ============================= #
# == ONE DAY, SEVERAL GROUPS == #
# ============================= #

# The user can use as many group names as required.
#
#: assert isID(ONE_USER_GRP_ABBREV) :#
ONE_USER_GRP_ABBREV:
#: if CONTEXT in [cor, done] :#
  - CONTEXT*:
    - Information is given point by point, with the option of using
      a dedicated DSL to automate classic use cases and extract
      some important data.
    - ...

  - todo:
#: elif DATE == next or isdate(DATE) :#
    - DATE:
      - See the specifications for the ''cor'' context.
